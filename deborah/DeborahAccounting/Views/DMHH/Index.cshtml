@model List<DeborahAccounting.Models.DMHH>
@{ ViewData["Title"] = "Danh mục hàng hóa"; }
<link rel="stylesheet" href="~/css/danhmuc.css" />

<div class="dm-container">
    <div class="dm-toolbar">
        <button class="btn btn-add" onclick="showAdd()"><i class="bi bi-plus-circle"></i> Thêm</button>
        <button class="btn btn-edit" onclick="showEdit()"><i class="bi bi-pencil"></i> Sửa</button>
        <button class="btn btn-delete" onclick="doDelete()"><i class="bi bi-trash"></i> Xóa</button>
        <button class="btn btn-reload" onclick="reload()"><i class="bi bi-arrow-clockwise"></i> Tải lại dữ liệu</button>
        <button class="btn btn-print"><i class="bi bi-printer"></i> In</button>
        <a class="btn btn-close-form" asp-controller="Home" asp-action="Index"><i class="bi bi-x-circle-fill"></i> Thoát</a>
    </div>
    <div class="dm-table-wrap">
        <table class="dm-grid">
            <thead><tr><th style="width:24px"></th><th>Mã hàng hóa</th><th>Tên hàng hóa</th><th>Đơn vị tính</th></tr></thead>
            <tbody id="gridBody">
                @foreach (var item in Model)
                {
                    <tr data-id="@item.MaHH" onclick="selectRow(this)">
                        <td class="row-indicator">▶</td>
                        <td>@item.MaHH</td><td>@item.TenHH</td><td>@item.Dvt</td>
                    </tr>
                }
                <tr class="new-row"><td class="row-indicator"><span class="required-star">✱</span></td><td></td><td></td><td></td></tr>
            </tbody>
        </table>
    </div>
</div>

<div class="modal fade dm-modal" id="formModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modalTitle">Cập nhật danh mục hàng hóa</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <input type="hidden" id="editMode" />
                <div class="mb-2 row align-items-center">
                    <label class="col-sm-4 col-form-label fw-bold">Mã hàng hóa</label>
                    <div class="col-sm-5"><input class="form-control" id="fMaHH" maxlength="10" /></div>
                </div>
                <div class="mb-2 row align-items-center">
                    <label class="col-sm-4 col-form-label fw-bold">Tên hàng hóa</label>
                    <div class="col-sm-8"><input class="form-control" id="fTenHH" maxlength="128" /></div>
                </div>
                <div class="mb-2 row align-items-center">
                    <label class="col-sm-4 col-form-label fw-bold">Đơn vị tính</label>
                    <div class="col-sm-5"><input class="form-control" id="fDvt" maxlength="10" /></div>
                </div>
            </div>
            <div class="modal-footer justify-content-end gap-2">
                <button class="btn btn-primary" onclick="save()"><i class="bi bi-floppy"></i> Lưu</button>
                <button class="btn btn-danger" data-bs-dismiss="modal"><i class="bi bi-x-circle-fill"></i> Hủy</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
<script>
let selectedId = null;
function selectRow(tr) { document.querySelectorAll('#gridBody tr').forEach(r => r.classList.remove('selected')); tr.classList.add('selected'); selectedId = tr.dataset.id; }
function showAdd() {
    document.getElementById('editMode').value = 'add';
    document.getElementById('modalTitle').textContent = 'Thêm hàng hóa';
    ['fMaHH','fTenHH','fDvt'].forEach(id => document.getElementById(id).value = '');
    document.getElementById('fMaHH').disabled = false;
    new bootstrap.Modal(document.getElementById('formModal')).show();
}
function showEdit() {
    if (!selectedId) { alert('Chọn một dòng để sửa'); return; }
    let tr = document.querySelector(`tr[data-id="${selectedId}"]`);
    document.getElementById('editMode').value = 'edit';
    document.getElementById('modalTitle').textContent = 'Cập nhật danh mục hàng hóa';
    document.getElementById('fMaHH').value = selectedId; document.getElementById('fMaHH').disabled = true;
    document.getElementById('fTenHH').value = tr.cells[2].textContent;
    document.getElementById('fDvt').value = tr.cells[3].textContent;
    new bootstrap.Modal(document.getElementById('formModal')).show();
}
function save() {
    let mode = document.getElementById('editMode').value;
    let url = mode === 'add' ? '@Url.Action("Create")' : '@Url.Action("Edit")';
    let data = { MaHH: document.getElementById('fMaHH').value, TenHH: document.getElementById('fTenHH').value, Dvt: document.getElementById('fDvt').value };
    fetch(url, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) })
    .then(r => r.json()).then(r => { if (r.success) { bootstrap.Modal.getInstance(document.getElementById('formModal')).hide(); reload(); } else alert(r.message); });
}
function doDelete() {
    if (!selectedId) { alert('Chọn một dòng để xóa'); return; }
    if (!confirm('Xác nhận xóa?')) return;
    fetch('@Url.Action("Delete")', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(selectedId) })
    .then(r => r.json()).then(r => { if (r.success) reload(); else alert(r.message); });
}
function reload() { location.reload(); }
</script>
}
